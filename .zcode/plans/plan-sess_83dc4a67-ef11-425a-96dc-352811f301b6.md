Compile the browser-crawler library's pure logic to WebAssembly and turn the existing playground/ Vite + React scaffold into a visual app that exercises the library in the browser (fetch a URL or paste HTML → view markdown IR, extracted links, forms/endpoints live).

1. Toolchain: verify/install wasm-pack and the wasm32-unknown-unknown Rust target.
2. Cargo.toml: add [lib] crate-type = ["rlib", "cdylib"]; move I/O deps (tokio, reqwest, headless_chrome, clap, futures, async-trait, serde_yaml) under [target.'cfg(not(target_arch = "wasm32"))'.dependencies]; add wasm-bindgen + serde-wasm-bindgen for wasm32; keep pure deps global.
3. src/lib.rs: cfg-gate I/O modules (builder, engine non-parser, exporter, output, pipeline, renderer, runner, stealth, mapper) so they only build natively; keep transformer, types, utils, engine::parser on both targets; fix stragglers (e.g. utils/formfill.rs std::fs usage).
4. New src/wasm.rs: #[wasm_bindgen] exports — transform(url, html) -> PageIR object, extract_links, extract_forms, version(); verify with cargo check on both native and wasm32 targets.
5. wasm-pack build --target web --out-dir playground/src/wasm; add "build:wasm" npm script.
6. Rewrite playground/src/App.tsx into the visualizer: input panel with Fetch URL (via a /api/fetch dev middleware added in vite.config.ts to bypass CORS) or Paste HTML mode; tabs for rendered Markdown + raw PageIR JSON, Links table (internal/external), Forms, Endpoints; wasm status badge; clean minimal CSS replacing the template scaffold.
7. Verify: pnpm build:wasm && pnpm build pass; run dev server, manually exercise all UI paths in the browser (fetch a real page, paste HTML, check every tab).
8. Docs: add a "Web / WASM playground" section to README.md with the build/run commands.